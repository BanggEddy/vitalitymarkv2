{# templates/admin/loyalty_card/newtype.html.twig #}

{% extends 'headeradmin.html.twig' %}

{% block body %}
    <h1>Add New Coupon</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Create</button>
    {{ form_end(form) }}
{% endblock %}
